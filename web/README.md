# web/ — Loopback review UI

Next.js + TypeScript review UI (Day 10-12). Upload a feedback batch, watch the agent's
step log, review the drafted issues, and **approve / reject** before anything is created in
GitLab. Calls the Cloud Run API only — no secrets client-side.

Scaffold with `npx create-next-app@latest .` here when starting Day 10.
