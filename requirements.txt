# Runtime Python deps for the Cloud Run container (see Dockerfile).
google-adk>=2.1
google-genai>=1.72,<2  # pinned by google-adk 2.1 (<2)
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
httpx>=0.27
pydantic>=2.0
google-cloud-secret-manager>=2.20  # OAuth token write-back (used when GITLAB_OAUTH_SECRET_RESOURCE set)
